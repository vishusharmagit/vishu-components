import bootstrapNaming from 'dummy/utils/bootstrap-naming';
import { module, test } from 'qunit';

module('Unit | Utility | bootstrap naming');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = bootstrapNaming();
  assert.ok(result);
});
