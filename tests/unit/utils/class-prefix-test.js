import classPrefix from 'dummy/utils/class-prefix';
import { module, test } from 'qunit';

module('Unit | Utility | class prefix');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = classPrefix();
  assert.ok(result);
});
