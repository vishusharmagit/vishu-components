import containsValue from 'dummy/utils/contains-value';
import { module, test } from 'qunit';

module('Unit | Utility | contains value');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = containsValue();
  assert.ok(result);
});
